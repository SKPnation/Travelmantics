<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ECF0F1"
    tools:context=".Authentication">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        app:popupTheme="@style/AppTheme.PopupOverlay" />

    <Button
        android:id="@+id/btnEmailSignIn"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="75dp"
        android:background="@drawable/email_signin_button"
        android:drawableLeft="@drawable/mail_drawable_left"
        android:drawablePadding="20dp"
        android:paddingLeft="26dp"
        android:paddingRight="60dp"
        android:text="Sign in with email"
        android:textAllCaps="false"
        android:textColor="#fff"
        android:textStyle="bold" />

    <Button
        android:id="@+id/btnGoogleSignIn"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_marginBottom="5dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:background="@drawable/google_signin_button"
        android:drawableLeft="@drawable/google_drawable_left"
        android:paddingRight="55dp"
        android:paddingLeft="6dp"
        android:gravity="center_vertical"
        android:text="Sign in with google"
        android:textAllCaps="false"
        android:textColor="#6C7A89"
        android:textStyle="bold"/>

</RelativeLayout>